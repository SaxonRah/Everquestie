# Public repository fixtures

The public repository intentionally omits personal EverQuest log captures and saved Allakhazam HTML fixtures. The production application does not require those files. Regression tests that depend on those local/private fixtures skip cleanly when the fixtures are absent.
